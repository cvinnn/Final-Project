# Final-Project

## Setup

1. Copy the environment template:
	- Duplicate .env.example and rename it to .env.
2. Fill in your API keys:
	- TWITTER_API_KEY
	- OPENROUTER_API_KEY

The notebook reads keys from environment variables, so keep .env private and do not commit it.